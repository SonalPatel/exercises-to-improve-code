//create a function to add two numbers together and output the results

// Declare the function and pa
function add(num1, num2) {
  return num1 + num2;
}
//declare the variable and give it a value
const result = add(5, 5);

//add the numbers and print to console and dispaly in an alertbox
console.log(result); //10
alert(result);
